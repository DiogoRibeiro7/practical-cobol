# Notes

This is the smallest useful COBOL program in the repository.

Important points:

- `PROGRAM-ID` names the program.
- `DISPLAY` writes output.
- `STOP RUN` ends the program.

Even this tiny example already shows a key COBOL trait: structure comes first.
