# Notes

This lesson uses COBOL arithmetic verbs.

## Important detail

The `GIVING` clause stores the result in a target field instead of overwriting one of the source fields.

## Why it matters

Business programs often keep original values and computed values separate. COBOL syntax makes that style explicit.
