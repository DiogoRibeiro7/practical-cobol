# Notes

- `PERFORM` is central to COBOL program organization.
- Paragraphs are often named to reflect business steps such as `READ-RECORD`, `VALIDATE-RECORD`, `UPDATE-TOTALS`, and `WRITE-REPORT-LINE`.
- Even before learning file handling, this pattern prepares you for batch processing.
