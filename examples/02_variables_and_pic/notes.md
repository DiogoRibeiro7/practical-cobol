# Notes

## `PIC X(20)`

This defines an alphanumeric field with 20 character positions.

## `PIC 999`

This defines a numeric field with three digits.

## What to notice

COBOL variables are strongly tied to layout and width. That is central to the language because many programs process structured records with fixed formats.
